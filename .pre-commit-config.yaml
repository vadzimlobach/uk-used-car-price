repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.5
    hooks:
      - id: ruff-check
        args: [--fix]
        exclude: artifacts/
      - id: ruff-format
        exclude: artifacts/